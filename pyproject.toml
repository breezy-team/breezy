[build-system]
requires = [
    "packaging",
    "setuptools>=60",
    "setuptools-gettext",
    "setuptools-rust",
    "configobj",
    "cython>=0.29",
    "pyyaml",
    "wheel",
]
build-backend = "setuptools.build_meta"
