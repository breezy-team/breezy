class SvnTransport(Transport):

class SvnBranch(ForeignBranch):
	. relpath contains path within repository

class SvnRepository(ForeignRepository):
	- contains notion of which paths are branches
	- UUID
	- num total revisions

class SvnWorkingTree(WorkingTree):

class SvnDirFormat(BzrDirFormat):
	open_workingtree() - not always available

