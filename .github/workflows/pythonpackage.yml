name: Python package

on: [push, pull_request]

jobs:
  build:

    continue-on-error: ${{ matrix.experimental }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: [3.7, 3.8, 3.9, "3.10", "3.11"]
        experimental: [false]
        include:
          - os: macos-latest
            experimental: true
            python-version: 3.7
          - os: windows-latest
            python-version: 3.7
            experimental: true
# See https://github.com/actions/toolkit/issues/399
#        include:
#          - os: ubuntu-latest
#            python-version: pypy3
#            experimental: true
      fail-fast: false

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies (apt)
        run: |
          sudo apt install quilt
        if: "matrix.os == 'ubuntu-latest'"
      - name: Install dependencies (brew)
        run: |
          brew install quilt
        if: "matrix.os == 'macos-latest'"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -U pip "setuptools>=60" setuptools-gettext cython setuptools-rust
          pip install -U pip coverage codecov cython testscenarios git+https://github.com/dulwich/dulwich
          pip install ".[dev,paramiko,doc,pgp,workspace,launchpad,git,fastimport]"
      - name: Install dependencies (linux/pip)
        run: |
          python -m pip install pyinotify
        if: "matrix.os == 'ubuntu-latest'"
      - name: Build docs
        run: |
          make docs PYTHON=python
      - name: Build extensions
        run: |
          make extensions PYTHON=python
        if: "matrix.python-version != 'pypy3'"
      - name: Test suite run (Linux)
        run: |
          ./brz selftest
        env:
          PYTHONHASHSEED: random
          BRZ_PLUGIN_PATH: -site:-user
          PYTHONPATH: .
        if: "matrix.os == 'ubuntu-latest'"
      - name: Test suite run (Mac OS)
        run: |
          ./brz selftest --subunit2 | subunit-filter --fixup-expected-failures=xfail.macos | subunit2pyunit
        env:
          PYTHONHASHSEED: random
          BRZ_PLUGIN_PATH: -site:-user
          PYTHONPATH: .
        if: "matrix.os == 'macos-latest'"
      - name: Test suite run (Windows)
        run: |
          ./brz selftest --subunit2 | subunit-filter --fixup-expected-failures=xfail.windows | subunit2pyunit
        env:
          PYTHONHASHSEED: random
          BRZ_PLUGIN_PATH: -site:-user
          PYTHONPATH: .
        if: "matrix.os == 'windows-latest'"
      - name: Run mypy
        run: |
          python -m pip install mypy types-PyYAML types-paramiko types-setuptools
          python -m mypy breezy
        if: "matrix.python-version != 'pypy3'"
