name: Rust

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install breezy
      run: sudo apt install -y python3-breezy
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
