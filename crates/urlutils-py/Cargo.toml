[package]
name = "urlutils-py"
version = "3.3.3"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = ">=0.18", features = ["extension-module"]}
breezy-urlutils = { path = "../urlutils" }
