Things to consider:
  * python_escape (maybe called string_escape) ala xml_escape?
  * some sort of block construct so easier to include in ReST and properties
    files, e.g.
    .. $begin-keywords$
    :name1: value1
    :name2: value2
    .. $end-keywords$
  * warn if bzr version too early?

* Add tests for:
  * global keyword values (including date formatting)
  * filters
  * escaping
  * style formatting
