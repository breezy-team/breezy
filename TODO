- construct working area:
   packages/
    configs/  - for each package


    $package ...

    $package/$debian

  ye olde arch-buildpackage layout

- create new package
  import from old source debs

- record a version

- build package

- new upstream

- migrate from arch-buildpackage?  use baz-import?

- make --merge work with different layouts of upstream tarballs.

- remove the need to specify the merge flag

- import of NMU diffs

- WRITE SOME TESTS
