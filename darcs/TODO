maybe later try to avoid a working dir in the exporter and use 'darcs
show contents --match "hash foo" path' + 'darcs chan --xml -s' output
instead of 'darcs appply'.

more intelligent tests, such as detect if the hg fastimport extension is
not enabled, etc.
