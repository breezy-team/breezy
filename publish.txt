Input:
 * base branch on hosting site
 * source branch somewhere else

Actions:
 * "fork" / clone / stack from base branch to a new branch
 * new branch is identified by:
  + owner (defaults to logged in user)
  + project name (derived from base branch)
  + branch name (derived from source branch name)
 * set public_branch variable

Open questions:
 *
