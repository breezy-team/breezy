language: python
sudo: false
cache: pip

python:
  - 3.3
  - 3.4
  - 3.5
  - 3.5-dev
  - 3.6
  - 3.6-dev
  - pypy3.3-5.2-alpha1

env:
  - PYTHONHASHSEED=random
    SELFTEST_OPTIONS="--load-list python3.passing"

matrix:
  include:
    - python: 2.7
      env: SELFTEST_OPTIONS=""

script:
  - make extensions docs
  - BRZ_PLUGIN_PATH=-site:-user python ./brz selftest --parallel=fork -q $SELFTEST_OPTIONS

install:
  - sudo apt install python-all-dev python3-all-dev
  - travis_retry pip install -U setuptools
  - travis_retry pip install -U pip coverage codecov flake8 testtools paramiko fastimport configobj cython testscenarios six docutils $TEST_REQUIRE

after_success:
  - python -m coverage combine
  - codecov
